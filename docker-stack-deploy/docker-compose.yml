version: "3"

services:
  pyapp:
    image: 127.0.0.1:5000/stackdemo
    build: .
    ports:
      - 8000:8000
    
  redis:
    image: redis:alpine

networks:
  secnet:
    driver: overlay
#    driver_opts:
      #- encrypted: 'true'